const consoleName = 'Antonio rulz';

console.log(consoleName);

// Adding a comment to test the removeComments compilation flag
const button = document.querySelector('button')!;
button.addEventListener('click', () => {
    console.log('Click!!');
});